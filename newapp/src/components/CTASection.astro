---
import { Button } from "$lib/components/ui/button";
import { BASE_URL } from "$lib/consts";
---

<section class="section1 text-center pt-[120px] pb-[140px] px-6 bg-ctaimage h-screen relative">
    <!-- Trusted Badge -->
    <div
        class="inline-flex border-t border-b border-[#fff] border-opacity-50 items-center bg-[#ffffff66] shadow-md px-5 py-[10px] rounded-full"
    >
        <span class=" mx-1 "></span>
        <p class="font-inter text-sm sm:small-text"><span class="font-bold">10+ Millions </span>Saved on Cloud for Clients</p>
    </div>

    <!-- Main Heading -->
    <h1
        class="main-heading-small md:main-heading mt-4 mb-6 max-md:text-center"
    >
        Struggling with Cloud's Expenses?
    </h1>

    <!-- Subtext -->
    <p class="mt-4 small-text md:text-para max-w-3xl mx-auto">
        Let us help you cut costs and boost savings. Start today and see the
        difference.
    </p>

    <!-- CTA Buttons -->
    <div class="mt-6 flex justify-center space-x-4">
        <Button href=`${BASE_URL}/contact-us` class="bg-black md:text-[16px] font-manrope">
            Get a Quote</Button
        >
    </div>
</section>
<style>
    .bg-ctaimage {
        background-image: url("../images/herodot.svg"), url("../images/cta.svg");
        background-size: cover, cover;
        background-position: center, center;
        background-repeat: no-repeat, no-repeat;
        height: 100%;
    }
</style>
